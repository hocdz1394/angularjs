import { ElementRef, EventEmitter, OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
export declare class ChartsDirective implements OnChanges, OnInit, OnDestroy {
    private el;
    chartType: 'line' | 'bar' | 'radar' | 'pie' | 'doughnut' | 'polarArea' | 'bubble' | 'scatter';
    chartLabels: string[];
    colors: any[];
    data: number[] | any[];
    datasets: any[];
    options: any;
    chartClick: EventEmitter<any>;
    chartHover: EventEmitter<any>;
    private chart;
    private ctx;
    private cvs;
    private initFlag;
    constructor(el: ElementRef);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): any;
    getChartBuilder(ctx: any): any;
    private refresh();
    private getDatasets();
    private updateChartData(newDataValues);
}
